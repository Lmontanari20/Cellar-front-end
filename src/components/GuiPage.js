import React, { Fragment } from "react";
import FormContainer from "./FormContainer";

const GuiPage = () => {
  return (
    <Fragment>
      <FormContainer />
      <div>cellarGui</div>
    </Fragment>
  );
};

export default GuiPage;
